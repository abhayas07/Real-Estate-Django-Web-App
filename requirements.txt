# Python 3.13 environment (verified working)
asgiref==3.8.1
Django==5.1.3
pillow==11.0.0
sqlparse==0.5.2

# Optional deployment extras — install with:
#   pip install gunicorn whitenoise
# gunicorn==20.0.4
# whitenoise==5.2.0
